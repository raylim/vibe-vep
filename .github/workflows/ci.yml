name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Test
        run: CGO_ENABLED=1 go test -race -cover -short ./...

      - name: Build
        run: CGO_ENABLED=1 go build -o vibe-vep ./cmd/vibe-vep
